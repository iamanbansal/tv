name: CI

on:
  push:
    branches: master
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      uses: actions/checkout@v2
      uses: actions/setup-java@v3
      with:
        java-version: 11

      name: build
      run: ./gradlew assembleDebug



